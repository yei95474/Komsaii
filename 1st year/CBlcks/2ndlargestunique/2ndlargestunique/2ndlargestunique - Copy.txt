#include <stdio.h>
#include <stdlib.h>
#include <math.h>
//Find the second largest unique number
int main()
{
    int size;
    printf("Enter size: ");
    scanf("%d", &size);
    int arr[size];
    int unique[size];
    int visited[size];
    int uniquesize=0;

    printf("Select Values:\n");
    for (int i=0;i<size;i++) {
        scanf("%d", &arr[i]);
    }
    printf("Your numbers are: ");
    for (int i=0;i<size;i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    for (int i=0; i<size; i++) {
        visited[i]=0;
    }

    for (int i=0; i<size; i++) {
        if (visited[i] == 1) {
            continue;
        }
        int count = 0;
        for (int j=0; j<size; j++) {
            if (arr[i]==arr[j]) {
                count++;
                visited[j]=1;
            }
        }
        if (count==1) {
            unique[uniquesize]=arr[i];
            uniquesize++;
        }
    } //random bullshit ass

    int second_highest = -9999999, highest_unique= -9999999;
    if (uniquesize>2) {
    for (int i=0;i<uniquesize;i++) {
        if (unique[i]>highest_unique) {
            highest_unique = unique[i];
        }
    }
    //for highest unique dawggggg ts so hard
    for (int i=0; i<uniquesize; i++) { 
        if (unique[i]>second_highest && unique[i]<highest_unique) {
            second_highest = unique[i];
         }
    }
    }
    else {
        printf("Insufficient unique numbers\n");
    }
    
    int pang_index_rani_bruh_kapoynako = second_highest, index=-1;

    for (int i=0; /*Kapoynakogang*/i<size;i++) {
        if (arr[i]==pang_index_rani_bruh_kapoynako) {
            index=i;
            break; /*niggaaaaaaaaaa*/
        }
    }

    if (index!=-1/*dili 0 kay 0 magsugod ang index yawaaaaaa*/) {
        printf("Second Largest: [%d]: %d\n", second_highest, index);
    }
    return 0;
}


/* for (int i=0;i<size;i++) {
        if (arr[i]>highest) {
            highest = arr[i];
        }
        if (arr[i]<lowest) {
            lowest=arr[i];
        }
    }*/